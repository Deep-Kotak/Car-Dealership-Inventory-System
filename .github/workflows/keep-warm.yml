name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send request to Render
        run: |
          curl -f https://car-dealership-inventory-system-mqub.onrender.com/health/ready